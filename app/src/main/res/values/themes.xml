<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.DotPot" parent="AppTheme_Normal">
        <!-- Primary brand color. -->
        <item name="colorPrimary">#003BFF</item>
        <item name="colorPrimaryVariant">#100917</item>
        <item name="colorOnPrimary">@color/colorTextPrimary</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">#003BFF</item>
        <item name="colorSecondaryVariant">#003BFF</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name = "android:windowBackground">@color/colorBackground</item>
        <item name="android:fontFamily">@font/font_text</item>
        <item name="fontFamily">@font/font_text</item> <!-- target android sdk versions < 26 and > 14 if theme other than AppCompat -->
        <item name="android:textColorHint">@color/colorTextSecondary</item>

    </style>
</resources>